{
    "size": 2,
    "delimiter": "|",
    "columns": [
        {
            "name": "column name 1",
            "required": true,
            "maxLength": 30,
            "minLength": 2,
            "values": ["ABC", "DEF"],
            "pattern": "/[A-Za-z0-9]+/"
        },
        {
            "name": "column name 2",
            "required": true,
            "maxLength": 30,
            "minLength": 2,
            "values": ["ABC", "DEF"],
            "pattern": "/[A-Za-z0-9]+/"
        }
    ]
}